<!DOCTYPE html>
<html lang="en">
    <head>
        <title></title>
        <link rel="stylesheet" type="text/css" href="{{ css }}">
    </head>
    <body>
        <div class="table">
            {% for row in table %}
                <div class="row">
                    {% for element in row %}
                        <div class="element">
                            {{ element }}
                        </div>
                    {% endfor %}
                </div>
            {% endfor %}
        </div>
    </body>
</html>